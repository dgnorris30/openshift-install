---
- hosts:
    - common_openshift_sys-dev1
  gather_facts: no
  tasks:
    - lineinfile:
        path: /etc/sysconfig/docker
        regexp: ^((?!#).*--block-registry[\s=]+docker\.io.*)$
        line: '#\1'
        backrefs: yes
